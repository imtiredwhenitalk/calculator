# Web Calculator - Науковий Калькулятор з Math Lab

## Опис
Вебзастосунок є **науковим калькулятором з інструментами для практичної математики**. Він поєднує функціональність обчислювального пристрою зі спеціалізованими математичними інструментами для розв'язування прикладних задач.

---

## 1. СТАНДАРТНІ АРИФМЕТИЧНІ ОПЕРАЦІЇ

### Базові операції
- **Додавання** (+)
- **Віднімання** (−)
- **Множення** (×)
- **Ділення** (÷)
- **Дорівнює** (=)

Калькулятор використовує **безпечне обчислення виразів** через бібліотеку Math.js, що запобігає помилкам синтаксису та уніфікує порядок виконання операцій.

---

## 2. НАУКОВІ ФУНКЦІЇ

### Тригонометрія
- **sin(x)** - синус (аргумент у радіанах)
- **cos(x)** - косинус
- **tan(x)** - тангенс
- **cot(x)** - котангенс (ctg)

### Логарифми и експоненціально-степеневі функції
- **log(x)** - десятковий логарифм (основа 10)
- **ln(x)** - натуральний логарифм (основа e)
- **√x** - квадратний корінь
- **x^y** - степенева функція (піднесення до степеня)
- **1/x** - обернена функція (реципрокал)

### Математичні константи
- **π (pi)** ≈ 3.14159... - число Пі
- **e** ≈ 2.71828... - число Ейлера (основа натурального логарифма)

### Порядок операцій
Калькулятор дотримується стандартного порядку операцій (PEMDAS):
1. Дужки ( )
2. Експоненти (показники степеня)
3. Множення и Ділення (зліва направо)
4. Додавання и Віднімання (зліва направо)

---

## 3. MATH LAB - ІНСТРУМЕНТИ ДЛЯ ПРАКТИЧНОЇ МАТЕМАТИКИ

### 3.1 ГРАФІКИ ФУНКЦІЙ

**Призначення:** Побудова графіків функцій однієї змінної для аналізу поведінки функції.

**Параметри введення:**
- **f(x)** - Функція в змінній `x` (наприклад: `sin(x)`, `x^2 + 2*x - 1`, `sqrt(x)`)
- **Діапазон X:** від мін до макс значення

**Методологія:**
- Генерується **240 точок** у діапазоні для плавної кривої
- Точки розраховуються sebagai: y = f(x) для кожного x
- Результат виводиться як **інтерактивний графік** (Chart.js)

**Приклади функцій:**
- `sin(x)` - синусоїда
- `cos(x)` - косинусоїда
- `tan(x)` - тангенс (вертикальні асимптоти при x = π/2 + πn)
- `cot(x)` - котангенс (вертикальні асимптоти при x = πn)
- `x^2` - парабола
- `1/x` - гіпербола
- `sqrt(x)` - коренева функція
- `exp(x)` - експоненціальна функція
- `x^3 - 3*x` - кубічна функція

**Графічне представлення:**
- Темна тема з видимими сіточками (клітинчаста сітка)
- Координатні осі X та Y проходять через центр (0, 0)
- Осі виділені яскравішим кольором (товщина 2px)
- Сітка з помірним кольором для кращої читабельності
- Легенда з назвою функції
- Інтерактивна взаємодія (наведення показує точні координати точок)
- Розриви графіка на асимптотах (tan, cot)

**Особливості для tan/cot:**
- Графік автоматично розривається біля асимптот
- 480 точок для плавної кривої
- Фільтрація екстремальних значень (>10⁶) для стабільності відображення

---

### 3.2 МАТРИЦІ - ОПЕРАЦІЇ ЛІНІЙНОЇ АЛГЕБРИ

**Призначення:** Виконання операцій над матрицями для розв'язування систем рівнянь та обчислень лінійної алгебри.

**Вибір розміру матриці:**
Доступні розміри: **2×2**, **3×3**, **4×4**, **5×5**

Кнопки розміру дозволяють швидко переключитися між різними розмірами матриць. При зміні розміру всі поля скидаються до нуля.

**Формат введення:**
Матриці відображаються у **візуальному вигляді** з круглими дужками та індивідуальними полями для кожного елемента.

**Приклад матриці 2×2:**
```
⎛  1   2  ⎞
⎝  3   4  ⎠
```

**Приклад матриці 3×3:**
```
⎛  1   0   0  ⎞
⎜  0   1   0  ⎟
⎝  0   0   1  ⎠
```

**Операції:**

#### Додавання матриць (A + B)
- Результат: матриця того ж розміру
- Операція поелементна: C[i,j] = A[i,j] + B[i,j]
- Використання: розв'язування систем лінійних рівнянь

#### Множення матриць (A × B)
- Умова: кількість стовпців A = кількість рядків B
- Результат: матриця розміру (строк A) × (стовпці B)
- Елемент результату: C[i,j] = Σ(A[i,k] × B[k,j])
- Використання: застосування лінійних перетворень, композиції

#### Визначник матриці (det A)
- Операція ТІЛЬКИ для квадратних матриць
- Математичний сенс:
  - det = 0 → матриця особлива (необоротна)
  - det ≠ 0 → матриця оборотна
- Використання: перевірка лінійної залежності,обчислення обсягів і площ

**Приклад обчислення визначника 2×2:**
```
A = [a b]    det(A) = a*d - b*c
    [c d]
```

---

### 3.3 ЧИСЛОВА ПОХІДНА

**Призначення:** Обчислення першої похідної функції у конкретній точці методом числового диференціювання.

**Параметри введення:**
- **f(x)** - Функція в змінній `x` (наприклад: `x^2 + 2*x`)
- **x₀** - точка, у якій обчислюється похідна

**Метод: Центральна різниця**
```
f'(x₀) ≈ [f(x₀ + h) - f(x₀ - h)] / (2h)
```
де h = 10⁻⁷ (малий крок для числової стійкості)

**Математичний сенс:**
- Похідна показує **миттєву швидкість зміни** функції
- Позитивне значення → функція зростає
- Негативне значення → функція спадає
- Нульове значення → точка «плато» (екстремум або перегину)

**Приклади:**
- Функція: `x^2`, точка: `2` → похідна ≈ 4.0 (теоретично 2*x = 2*2 = 4)
- Функція: `sin(x)`, точка: `0` → похідна ≈ 1.0 (теоретично cos(0) = 1)
- Функція: `x^3`, точка: `1` → похідна ≈ 3.0 (теоретично 3*x² = 3*1² = 3)

**Точність методу:**
- Числова похідна наближає аналітичну похідну
- Помилка: O(h²) - дуже висока точність для практичного використання
- Стає неточною на розривах функції

---

### 3.4 КІНЦЕВА СУМА

**Призначення:** Обчислення суми виразу над послідовністю цілих чисел.

**Параметри введення:**
- **Вираз в n** - формула залежно від змінної `n` (наприклад: `n^2`, `2*n + 1`, `1/n`)
- **Початок** - стартове значення n
- **Кінець** - останнє значення n

**Математична формула:**
```
Сума = Σ(вираз) де n від початку до кінця
     = вираз(початок) + вираз(початок+1) + ... + вираз(кінець)
```

**Приклади:**

1. **Сума перших n натуральних чисел**
   - Вираз: `n`
   - Від 1 до 100
   - Результат: 1 + 2 + 3 + ... + 100 = 5050

2. **Сума квадратів**
   - Вираз: `n^2`
   - Від 1 до 10
   - Результат: 1 + 4 + 9 + 16 + ... + 100 = 385

3. **Геометричний ряд**
   - Вираз: `0.5^n`
   - Від 1 до 20
   - Результат: 0.5 + 0.25 + 0.125 + ... ≈ 1.0 (наближається до 1)

4. **Факторіли (факторіальна сума)**
   - Вираз: `n!` (якщо Math.js підтримує)
   - Від 1 до 10
   - Результат: 1! + 2! + 3! + ... + 10! = 4037880

**Застосування в прикладній математиці:**
- Рядові розкладення (Taylor, Fourier)
- Інтегральні наближення (методи прямокутників)
- Статистичні розрахунки (сума, середнє, дисперсія)
- Комбінаторні задачі

---

## 4. КОРИСТУВАЛЬНИЦЬКИЙ ІНТЕРФЕЙС

### Двопанельна архітектура
Калькулятор розділений на два основні вкладки:

#### 1️⃣ **Калькулятор** - основна в'їжджають з кнопками
- Цифри: 0-9
- Операції: +, −, ×, ÷
- Наукові функції: sin, cos, tan, log, ln, √, x^y, 1/x, π, e
- Управління: AC (очистити), DEL (видалити останній символ)
- Дисплей: показує поточний вираз і результати

#### 2️⃣ **Math Lab** - чотири спеціалізовані інструменти
- Графіки функцій
- Матричні операції
- Числова похідна
- Кінцева сума

### Мовна підтримка
- 🇺🇦 **Українська** - повний переклад інтерфейсу
- 🇬🇧 **English** - англійський варіант
- **Пам'ять вибору** - вибір мови зберігається в браузері (localStorage)

### Темна тема
- **Темний фон** з голубими акцентами
- **Плавні анімації** кнопок при натисканні
- **Адаптивний дизайн** - оптимізовано для мобільних пристроїв та ПК

---

## 5. ТЕХНІЧНІ ХАРАКТЕРИСТИКИ

### Математична обчислювальна мотор
- **Бібліотека:** Math.js
- **Версія:** 11.11.2
- **Функціональність:** Безпечне обчислення виразів, розміри матриць до 100×100

### Графічна бібліотека
- **Бібліотека:** Chart.js
- **Версія:** 4.4.1
- **Графіки:** Інтерактивні, з можливістю приблизити/видалити

### Стиль і дизайн
- **CSS Framework:** Tailwind CSS (CDN)
- **Анімації:** CSS3 переходи і трансформації
- **Адаптивність:** Mobile-first підхід, працює на екранах від 320px до 4K

### Обчислювальна складність
- **Графіки:** O(n) де n = 480 точок (збільшено для кращої якості тригонометричних функцій)
- **Матричні операції:** 
  - Додавання: O(n²) для матриці n×n
  - Множення: O(n³) наївна реалізація
  - Визначник: залежить від розміру (підтримка 2×2, 3×3, 4×4, 5×5)
- **Похідна:** O(2) - два обчислення функції
- **Сума:** O(n) де n = кількість членів

---

## 6. КОМ ВИКОРИСТАННЯ

### Запуск
1. Відкрити файл `main.html` у будь-якому сучасному браузері
2. Мотор відразу готовий до роботи

### Приклади типових розрахунків

#### Приклад 1: Обчислення синуса
```
1. Натиснути: sin
2. Натиснути: (
3. Натиснути: π
4. Натиснути: /
5. Натиснути: 4
6. Натиснути: )
7. Натиснути: =
Результат: 0.7071... (sin(π/4) = √2/2)
```

#### Приклад 2: Побудова графіку параболи
```
1. Перейти на вкладку "Math Lab"
2. Натиснути вкладку "Графіки"
3. Ввести f(x): x^2 - 4*x + 3
4. Встановити X від -5 до 5
5. Натиснути кнопку "Побудувати"
→ Видно параболу з коренями при x=1 і x=3
```

#### Приклад 2a: Графік тангенса
```
1. На вкладці "Графіки"
2. Ввести f(x): tan(x)
3. Встановити X від -3.14 до 3.14
4. Натиснути "Побудувати"
→ Видно періодичний графік з вертикальними асимптотами при x = ±π/2
```

#### Приклад 2b: Графік котангенса
```
1. На вкладці "Графіки"
2. Ввести f(x): cot(x)
3. Встановити X від 0.1 до 6.28
4. Натиснути "Побудувати"
→ Видно графік з асимптотами при x = π, 2π (кратних π)
```

#### Приклад 3: Додавання матриць
```
1. На вкладці "Матриці"
2. Оберіть розмір: 2×2
3. Заповніть Матрицю A:
   ⎛ 1  2 ⎞
   ⎝ 3  4 ⎠
4. Заповніть Матрицю B:
   ⎛ 5  6 ⎞
   ⎝ 7  8 ⎠
5. Натисніть "A + B"
6. Результат:
   ⎛  6   8  ⎞
   ⎝ 10  12  ⎠
```

#### Приклад 4: Обчислення похідної
```
1. На вкладці "Похідна"
2. Функція: x^3 - 2*x
3. x₀: 2
4. Результат: f'(2) ≈ 10 (теоретично 3*2² - 2 = 10)
```

#### Приклад 5: Сума геометричного ряду
```
1. На вкладці "Сума"
2. Вираз: (0.5)^n = 0.5^n
3. Від: 1
4. До: 30
5. Результат: ≈ 0.9999997 (наближається до 1)
```

---

## 7. ОБМЕЖЕННЯ І ПРИПУЩЕННЯ

### Математичні обмеження
- **Область визначення:** Користувач повинен забезпечити коректні значення (наприклад, не ділити на 0, не брати логарифм від'ємного числа)
- **Точність:** До ~15 знаків після коми (обмеження JavaScript)
- **Матриці:** Розміри 2×2, 3×3, 4×4, 5×5 (оптимізовано для візуального введення)

### Функціональні обмеження
- **Похідна:** Числова (не аналітична) - залежить від обраного кроку h
- **Сума:** Тільки для цілих чисел n (не для неперервних функцій)
- **Тригонометрія:** Аргумент у **радіанах**, не в градусах

### Браузерні вимоги
- Сучасний браузер з підтримкою ES6 JavaScript
- Доступ до інтернету для завантаження CDN бібліотек (Math.js, Chart.js)
- Включений JavaScript (вимога)

---

## 8. МЕТОДОЛОГІЧНА ЦІННІСТЬ

### Для навчання предметів прикладної математики
1. **Обчислювальна математика** - числові методи (диференціювання, інтегрування через суми)
2. **Лінійна алгебра** - матричні операції, системи рівнянь
3. **Математичний аналіз** - дослідження функцій, похідні, графіки
4. **Чисельні методи** - наближені обчислення з забезпеченням помилок

### Практичні застосування
- **Інженерні розрахунки** - аналіз функцій поведінки систем
- **Економічні моделі** - допускаємо матричні розрахунки
- **Фізичні симуляції** - обчислення похідних для швидкостей і прискорень
- **Статистичні аналізи** - суми для обчислення статистичних показників

---

## 9. ФАЙЛОВА СТРУКТУРА

```
web/calculator/
├── main.html      # Структура UI (HTML5 семантичний)
├── script.js      # Логіка у JavaScript (340+ строк)
├── style.css      # Стилізація та анімації (356+ строк)
└── README.md      # Цей файл
```

### Архітектура JavaScript
- **Translocation система** - ~100 ключів для двох мов
- **Безпечне обчислення** - через Math.js.evaluate()
- **Управління скаутом** - localStorage для мови
- **Модульна структура** - окремі функції для кожного Math Lab інструменту

---

## 10. АЛЬТЕРНАТИВА: PYTHON TKINTER ВЕРСІЯ

Окрім веб-версії, існує також **портативна десктопна версія** калькулятора, написана на Python з Tkinter.

### Порівняння двох версій

| Характеристика | Веб-версія | Python Tkinter |
|---|---|---|
| **Платформа** | Браузер | Локальна ОС (Windows/Mac/Linux) |
| **Запуск** | Безпосередньо у браузері | Потрібен Python 3.8+ |
| **Основні функції** | ✅ Науковий калькулятор | ✅ Науковий калькулятор |
| **Графіки** | ✅ Вбудовані (Chart.js) | ✅ Вбудовані (Tkinter Canvas) |
| **Матриці** | ✅ Додавання, множення, det | ❌ Не реалізовано |
| **Похідна** | ✅ Числова (центральна різниця) | ❌ Не реалізовано |
| **Суми** | ✅ Кінцевої суми | ❌ Не реалізовано |
| **Math Lab інструменти** | ✅ 4 спеціалізовані вкладки | ❌ Графік тільки |
| **Мовна підтримка** | ✅ UA/EN з localStorage | ❌ Інтерфейс англійською |
| **Теми оформлення** | ✅ Темна (спеціально для UI) | ✅ Темна/Світла (анімована) |
| **Поскладність** | Легко для веб-пошука | Потрібна інстальованість Python |
| **Затримка запуску** | Миттєво (вже в браузері) | ~2-3 сек (запуск Tkinter вікна) |

### Python Tkinter версія - Особливості

**Меню:** `python/any/calculator.py`

#### Базовий інтерфейс
- **Ліва панель:** Графік функції з інтерактивним полотном (Canvas)
- **Права панель:** Калькулятор з кнопками та дисплеєм

#### Наукові функції (як у веб-версії)
- Тригонометрія: **sin(x)**, **cos(x)**, **tan(x)**, **cot(x)**
- Логарифми: **log(x)** (основа 10), **ln(x)** (натуральний)
- Степеневі: **√x**, **x^2**, **x^y**, **1/x**
- Константи: **π**, **e**

#### Встроена функція графіків
- Побудова графіків функцій однієї змінної
- В ручну налаштовуємо діапазон X (мін/макс)
- Гладка крива з 100+ розраховуваних точок
- Осі, сітка, легенда функції

#### Оформлення і анімація
- **Переключення теми:** `t` в фокусі вікна або клік кнопки
- **Анімація:** Плавний перехід між темами (14-кроковий анімаційний цикл)
- **Кольорова схема:** Temna тема з голубими акцентами для наукового виглядку
- **Кнопки:** Округлені прямокутники з эффектом hover та click

#### Управління клавіатурою
```
Цифри:        0-9
Операції:     + - * / =
Функції:      s (скрити/показати наукову панель)
Тема:         t (переключити темну/світлу)
Очистка:      c (очистити дисплей)
Backspace:    Розраховуємо та видаляємо останній символ
Enter:        Виконати обчислення
```

#### Технічні деталі Python версії
- **Framework:** Tkinter (стандартна бібліотека Python)
- **Платформа:** Windows, macOS, Linux
- **Залежності:** Тільки `math` модуль
- **Розмір коду:** ~830 рядків
- **Графіка:** Native Tkinter Canvas для побудови графіків

---

## 11. РЕКОМЕНДАЦІЇ ДЛЯ ВИБОРУ ВЕРСІЇ

### Використовуйте **Веб-версію**, якщо:
- Потрібен **швидкий доступ** без встановлення
- Хочете використовувати **Math Lab інструменти** (матриці, похідні, суми)
- Потребуєте **мобільну версію** (смартфон/планшет)
- Потрібна **мульติязична підтримка** (укр/англ)
- Потрібна возможність **легко поділитися** (просто посилання на файл)

### Використовуйте **Python Tkinter версію**, якщо:
- Бажаєте сценарій **без інтернету** (офлайн режим)
- Потрібна **найменша затримка** при запуску
- Працюєте в **ізольованому середовищі** без браузера
- Бажаєте **модифікувати код** та розширювати функціональність
- Потрібна потуга **системної інтеграції** (ярлик на десктопі, контекстне меню, тощо)

---

## 12. ЗАКЛЮЧНА ІНФОРМАЦІЯ

Обидві версії калькулятора розроблена для **студентів прикладної математики**, інженерів та науковців, які потребують лінивого та потужного розрахункового інструменту.

**Веб-версія** забезпечує компромісне рішення з найсучаснішими математичними інструментами та доступністю.
**Python-версія** пропонує нативний десктопний досвід з оптимальною продуктивністю.

**Розроблено для:** Студентів прикладної математики, інженерів, науковців
**Типи:** 
- Веб-застосунок (HTML5 + JavaScript + CSS3)
- Десктопна програма (Python 3.8+ + Tkinter)
**Ліцензія:** Open source

# Приклади Функцій для Графіка

## Лінійні функції
```
x
2*x + 3
-x + 5
0.5*x - 2
```

## Квадратичні (параболи)
```
x^2
x^2 - 4
-x^2 + 2*x + 3
2*x^2 - 8*x + 6
```

## Кубічні
```
x^3
x^3 - 3*x
x^3 - 2*x^2 + x
```

## Тригонометричні
```
sin(x)
cos(x)
tan(x)        # Діапазон: -3.14 до 3.14
cot(x)        # Діапазон: 0.1 до 6.28
sin(x) + cos(x)
2*sin(x)
sin(2*x)
```

## Експоненціальні та логарифмічні
```
exp(x)        # e^x
2^x
log(x)        # Діапазон: 0.1 до 100
ln(x)         # Діапазон: 0.1 до 10
```

## Раціональні (дроби)
```
1/x           # Діапазон: -10 до 10 (пропускає 0)
1/(x^2)
x/(x+1)
(x^2-1)/(x^2+1)
```

## Кореневі
```
sqrt(x)       # Діапазон: 0 до 10
sqrt(x^2-4)   # Діапазон: -5 до 5
x^(1/3)       # Кубічний корінь
```

## Комбіновані
```
x*sin(x)
exp(-x^2)
sin(x)/x
x^2*cos(x)
sqrt(x)*sin(x)
```

## Кусково-лінійні (з abs)
```
abs(x)
abs(x-2)
abs(sin(x))
```

## Рекомендовані діапазони

| Функція | X від | X до |
|---------|-------|------|
| Лінійні | -10 | 10 |
| Квадратичні | -5 | 5 |
| Кубічні | -3 | 3 |
| sin/cos | -6.28 | 6.28 |
| tan | -3.14 | 3.14 |
| cot | 0.1 | 6.28 |
| exp | -3 | 3 |
| log/ln | 0.1 | 10 |
| 1/x | -10 | 10 |
| sqrt | 0 | 10 |

## Цікаві графіки

### Серце (heart curve)
```
abs(x)^(2/3) + 0.9*sqrt(3.3-x^2)*sin(10*pi*x)
# Діапазон: -2 до 2
```

### Гаусова крива
```
exp(-x^2)
# Діапазон: -3 до 3
```

### Загасаюча синусоїда
```
exp(-x)*sin(5*x)
# Діапазон: 0 до 5
```

### Функція Діріхле (ступінчаста)
```
floor(x)
# Діапазон: -5 до 5
```

### Сигмоїда
```
1/(1+exp(-x))
# Діапазон: -6 до 6
```
